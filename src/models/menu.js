class Menu {
    constructor(id, screen, title, imageUrl) {
        this.id = id;
        this.screen = screen;
        this.title = title;
        this.imageUrl = imageUrl;  
    }
} 

export default Menu;