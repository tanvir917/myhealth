import React, {useState, useEffect} from 'react';
import { 
    ScrollView,
    View,
    Text,
    Image,
    Button
 } from 'react-native';

const doctorsList = props => {
     return (
        <View>
            <Text>Hello</Text>
      </View>
     );
 }
 export default doctorsList;